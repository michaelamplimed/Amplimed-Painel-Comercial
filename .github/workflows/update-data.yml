name: Atualizar dados do Painel Amplimed

on:
  schedule:
    # A cada 30 minutos, das 07:00 às 19:00 no horário de Brasília.
    # Brasília = UTC-3, portanto 10:00 às 22:00 UTC.
    - cron: "*/30 10-22 * * *"

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: painel-amplimed-update
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código do repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Buscar dados atualizados do HubSpot
        env:
          HUBSPOT_TOKEN: ${{ secrets.HUBSPOT_TOKEN }}
        run: node scripts/fetch-hubspot-data.js

      - name: Verificar se houve alteração nos dados
        id: changes
        run: |
          if git diff --quiet -- data/data.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commitar dados atualizados
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/data.json

          git commit -m "chore: atualiza dados do painel via HubSpot"

          # Evita falha "fetch first" quando alguem deu push manual entre o checkout e o push do bot.
          # Tenta ate 5x: rebase sobre o main remoto e reenvia.
          for i in 1 2 3 4 5; do
            if git push; then
              echo "Push ok na tentativa $i"
              break
            fi
            echo "Push falhou (tentativa $i), fazendo pull --rebase e tentando de novo..."
            git pull --rebase origin main
            if [ "$i" = "5" ]; then
              echo "Falha apos 5 tentativas"
              exit 1
            fi
          done
